# Design system inventory (existing)

<!-- Catalog everything in the Figma file that Claude can reuse or must know about -->

## Conventions

<!-- Describe how this file is organized and how components are named -->

- **Naming scheme**: (e.g., "Atomic/[Atomic]/[Component]/[Variant]")
- **Where components live**: (e.g., "Assets panel, organized by type; main components on /Components page")
- **Variable collections & modes**: (e.g., "Color (light/dark modes), Spacing (mobile/desktop)")
- **Text styles**: (e.g., "Prefixed by role: Display/, Heading/, Body/, Caption/")
- **Effect styles**: (e.g., "Shadow/, Blur/")

## Components

| Component | Atomic level | Node ID / key | Variants & properties | Use it for | Don't use it for |
|-----------|--------------|----------------|----------------------|------------|------------------|
| (example — delete) | Atom/Molecule/Organism | (ID) | (e.g., "size: sm/md/lg, state: default/hover/disabled") | (description) | (gotchas) |

## Tokens

<!-- Table of variable collections in the file -->

| Collection | Modes | What it holds | Naming pattern |
|-----------|-------|---------------|-----------------|
| (example — delete) | light, dark | (colors/spacing/sizing) | (e.g., "color/primary/background") |

## Tokens added by Claude

<!-- Log any new variables or styles Claude created to fill gaps in the system -->

| Date | Variable/style | Collection | Values per mode | Why it was needed |
|------|-----------------|-----------|-----------------|-------------------|
| 2026-09-21 | (example — delete) | (collection name) | light: X, dark: Y | (gap it fills) |

## Gaps

<!-- Things the system lacks that flows will need (new tokens, missing components, interactions, etc.) -->

- 
